type Approval {
    user: Boolean!
    password: Boolean!
}

type User {
    id: String!
}

type Query {
    info: String!
    tkn(key: String!): User!
}

type Mutation {
    login(
        user: String!
        password: String!
    ): Approval!
    register(
        name: String!
        nickname: String!
        birthday: String!
        email: String!
        password: String!
    ): ID!
}