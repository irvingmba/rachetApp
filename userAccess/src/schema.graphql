type Approval {
    user: Boolean!
    password: Boolean!
}

type Query {
    info: String!
}

type Mutation {
    login(
        user: String!
        password: String!
    ): Approval!
    register(
        name: String!
        nickname: String!
        birthday: String!
        email: String!
        password: String!
    ): ID!
}