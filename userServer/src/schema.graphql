type Query {
    info: String!
}

type Mutation {
    login(
        user: String!
        password: String!
    ): Boolean!
    register(
        name: String!
        nickname: String!
        birthday: String!
        email: String!
        password: String!
    ): ID!
}